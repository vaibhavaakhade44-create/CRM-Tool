{
  "name": "crm-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist",
    "start": "cd frontend && npm run preview"
  }
}
